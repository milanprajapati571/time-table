import React from 'react';

const Header = () => (
    <header className="text-center mb-8">
        <h1 className="text-4xl font-bold text-black-600">Course Timetable Builder</h1>
        <p className="text-gray-600 mt-2">Select your courses to generate a personalized weekly schedule.</p>
    </header>
);

export default Header;
